apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: wandoor-database
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/kelompok-3-odp343/infra.git
    targetRevision: staging
    path: k3s/database-wandoor
  destination:
    server: https://kubernetes.default.svc
    namespace: wandoor
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
